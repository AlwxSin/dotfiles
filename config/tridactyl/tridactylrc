" remap next and prev tab
bind J tabnext
bind K tabprev

" Smooth scroll
bind j scrollpx 0 40
bind k scrollpx 0 -40
bind <C-d> scrollpx 0 200
bind <C-u> scrollpx 0 -200

" search on page
bind / fillcmdline find
bind ? fillcmdline find -?
bind n findnext 1
bind N findnext -1
bind ,<Space> nohlsearch

" back and next page in history
bind [[ back
bind ]] forward
